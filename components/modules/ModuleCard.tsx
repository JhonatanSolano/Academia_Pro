"use client";

/**
 * @deprecated — Replaced by ProgramCard in the student dashboard page.
 * Kept as a stub to avoid import errors during migration cleanup.
 */
export default function ModuleCard() {
  return null;
}
